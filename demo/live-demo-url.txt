NOT DEPLOYED — run locally using docs/setup-guide.md

The project is evaluated locally. To run:
  1. cd src/backend && pip install -r requirements.txt
  2. python -m uvicorn app.main:app --reload --port 8000
  3. cd src/frontend && npm install && npm run dev
  4. Open http://localhost:5173
